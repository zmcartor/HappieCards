describe 'Choose Background tile-view' , ()->
  it 'contains a background collection', ()->
    # set category id
    # test correct URL is polled
    expect(1).toEqual(1)

  it 'responds to a change in category dropdown', ()->
    # ensure the collection can be instantiated
    expect(1).toEqual(1)

  it 'renders correct number of choose background tiles' , ()->
    expect(1).toEqual(1)
