describe 'Background single tile view' , ()->
  it 'shows preview when button clicked', ()->
    # set category id
    # test correct URL is polled
    expect(1).toEqual(1)

  it 'pops event when background chosen', ()->
    # ensure the collection can be instantiated
    expect(1).toEqual(1)

  it 'renders correct number of choose background tiles' , ()->
    # render tile called X times
    expect(1).toEqual(1)
