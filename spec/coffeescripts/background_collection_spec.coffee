describe 'Backgrounds collection' , ()->
  it 'fetches backgrounds from a specific category id', ()->
    # set category id
    # test correct URL is polled
    expect(1).toEqual(1)

  it 'Holds a collection of background models', ()->
    # ensure the collection can be instantiated
    expect(1).toEqual(1)
