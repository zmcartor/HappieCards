window.HappieApp ||= {}
window.HappieApp.Views ||= {}

window.HappieApp.Views.Card_compose = class Card_compose extends Backbone.View
  # Listens to the buttons within the page and pops appropriate events.
  # That's all.
